# boomPI项目约定

## 目录与范围

项目只保留正式客户端、配套服务端、协议、硬件约束、必要资源和构建部署入口。客户端唯一main.cpp位于client/apps/boompi_client。不要重新加入测试、替身、模拟器、验证报告、临时探针或统计脚手架。

2026-09-19语音体验已由用户验收，作为教学冻结基线；后续修改以用户明确的新需求或已定位缺陷为范围。

服务端是课程配套黑箱，学生只配置Qwen/DashScope Key。教学重点为BSP、板端C/C++、音频、线程通信、界面和故障排查，不扩展Go或云端SDK课程。

## 代码组织

- 主流程顺着读取、处理、交付展开；模块对外保留一级namespace，文件内部函数和变量使用static。
- 不恢复VoiceAudio/AudioEngine/AudioBackend聚合转发，不新增Manager或通用消息框架。
- 同一状态有明确所有者：应用管对话，网络管轮次/序号，播放管声卡消费，UI管LVGL对象。
- 自有终端输出集中在debug.cpp注册的只读回调表；调用点只交付事件和参数。错误返回、内存错误原因和配置文件写入不属于终端日志。
- 自有具名回调及日志回调表字段统一使用_cb后缀，声明、注册和调用点保持一致；第三方API名称保持原版。
- main无参数直接运行，只组织App_Init、App_Process循环和App_Close。配置读取、进程锁、退出通知及失败结果由现有应用模块管理；不恢复clientctl、启动命令分支或脚本注入配置。
- 入口参考ESP32小智的顺序初始化风格；Linux主循环必须持续到停止/故障再回收，不能初始化后直接返回导致全部线程退出。App_Close返回0正常、1故障，必要错误由日志回调报告。
- 头文件只暴露必要契约，第三方类型留在底层。使用C++17、根.clang-format和自然中文注释；一行一条语句，不压排版凑行数。
- 教学写法只参考XIAOZHI_CODE正式App/Inf/Driver业务源码：顺序初始化、显式状态分支、就地检查返回值。不读取或模仿其test/text示例。使用一级模块namespace、static内部实现、四空格缩进和换行花括号；保持Linux现有线程与缓冲职责。
- 注释直接解释每个处理步骤的意义、必要性和数据去向：文件说明职责与线程，接口说明输入输出和调用前提，关键分支说明原因。使用正向、自然中文，清除比较旧实现和叙述修改历史的文字。
- 自有普通函数省略多余noexcept标记；第三方及回调签名按实际接口匹配。
- 普通业务失败用bool/小枚举；不主动throw作流程跳转。异常捕获只放在实际会抛异常的库/线程创建/应用边界，说明来源，避免包住不抛异常的普通流程。
- 固定板级参数与SDK约束用就近注释说明，不堆叠assert、类型签名模板或魔数断言；仍检查实际I/O失败、数据长度及缓冲边界。第三方代码不修改。
- 自有常量使用普通const；实现文件内部常量使用static const。数组长度和模板参数由整型常量表达式提供，第三方声明保持原版。
- 主循环按回复、输入、触摸展开，状态不适用时提前返回。等待条件只判断，不在条件变量的谓词里修改业务状态；正文和尾播在调用处写明。
- 网络按发现、WSS握手、收发、重连顺序实现；系统服务负责DHCP、Wi-Fi和路由。精简时保留TLS、背压、取消、连续性与旧轮隔离；3A参数保持原值。

## 音频与资源

- 保留唤醒、3A、VAD、500ms前滚、流式问答、同句插话、追问、音量和断线恢复。
- 保持当前48k四通道采集、16k算法/协议、48k双声道播放。硬件路径和3A API见docs/hardware/README.md。
- wake.cpp单独旧C++ ABI；官方及第三方源码、模型和库不修改。
- 两路PCM配置好后才开始采集；退出先中断/join线程，再关闭设备与算法。
- 算法间不排队，网络等待不进入实时采集。队列有界，不能静默丢PCM或掩盖断点。
- 初始化后持续顺序处理输入；播放插话累计连续300ms当前帧VAD命中，普通监听及句尾保留语音延续。DONE和实际尾播结束严格区分。
- 采集初始化写入并回读已验收的ADC模拟增益23和高通On。板型及声学结构变化后重新验证，不能仅靠修改VAD计时掩盖输入失真。
- 不凭猜测修改声道、极性、AEC延迟、GPIO、寄存器或复位等待。

## UI与网络

- 只保留小智单页与触摸，包含表情、状态、字幕、开始/停止及音量。UI线程独占运行期LVGL，快照在短锁内交接。
- 音量拖动实时更新，释放时由UI线程保存。不恢复摄像头或额外页面。
- 课程镜像预先配置系统联网与路由；客户端在已联网子网发现服务端，不启动DHCP/Wi-Fi子进程或选择网卡。Wi-Fi凭据保存在板端权限0600的系统配置，不进入源码或日志。
- WSS保留TLS/SPKI校验、hello口令、背压、超时、取消和旧轮隔离。
- 程序在/run持有单实例文件锁，退出由内核释放；不得让重复进程争抢声卡或屏幕。SIGINT/SIGTERM走正常资源回收。
- 服务端身份稳定保存。Key、私钥、真实配置、原始PCM及完整对话不提交Git。

## 构建与交付

- 第三方依赖集中在third_party：先git clone固定上游版本，再阅读API并开发接入。嵌套仓库与构建产物不提交父仓库；公开库源码、上游预编译库、SDK专有库必须明确区分。
- 不再引用OpenSSL/Snowboy/VAD的临时构建目录。依赖位置由third_party固定布局提供，工具链与sysroot仍来自匹配的幸狐SDK。
- 使用匹配的幸狐SDK/GCC/uClibc。优先复用已有BOOMPI_*、CMakeUserPresets.json和已记录构建主机；不要把缺少当前终端变量误判为没有SDK。
- 构建入口为rv1106-release preset与scripts/build_release.sh。检查ARM架构、loader、依赖和RPATH，不用Host程序替代ARM产物。
- 保留已有修改，不强推、不重写历史。升级前确认目标板和恢复副本，保留设备配置及TLS身份。
- 刷镜像、修改分区/DTS需用户明确授权。编译成功不代替真实显示、触摸、网络和声学体验确认。

- 提交、推送和板端部署按用户当前指示执行；分别记录源码版本、构建验证和人工体验结果，并提供准确的版本链接。
