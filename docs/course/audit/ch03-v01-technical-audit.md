# 第3章 v0.1 技术事实终审

## 最终候选定点复核（2026-08-10 21:03）

**PASS（P0 = 0，P1 = 0）**。本节是当前最新结论，取代下方两轮 REJECTED 历史记录。

放行基线：

- Markdown SHA256：`F2F680A6E0BB46DC9DEAF4CAC2424366143F86C77347567123004ED959DBFB3B`
- `sources/README.md` SHA256：`873E4E19B4F9E361F24B51DE5180E68C1A78A7174AB40DD0D60180B061A98245`
- 6页 PDF SHA256：`5D5D2540EAE2C4B2DD7AAA641D630C8B6BEA9AEA50B748891BB59A6B5BFDCECA`

分页定点回归（2026-08-10 21:06）：仅在图3-3前新增 `[[PAGEBREAK]]`，技术内容无变化、6页渲染无回归；新放行基线为 Markdown SHA256 `3A59CA2F245B58F2D058ADBCE21C9E5EA26A562955E5C7C3F19148B32C4F914F`、PDF SHA256 `758BE3A499FF1858CEA50AEF21A1BB4E2B183A7BC45EACEF3C4800E4016DD294`。

最终图文定点回归（2026-08-10 21:09）：图3-3仅重制为紧凑六框横条，章末仅压缩同义句；BootROM、引导程序、Linux内核、rootfs/init和用户空间的职责边界，以及“仅已绑定驱动、其他外设仍须逐项测试”的结论均无退化，5页PDF渲染通过，**技术PASS继续有效**；新放行基线为 Markdown SHA256 `C0AA483F39807AAF671FC6A1401BE641002C88CB56F3CB95406E9D803A2FA75E`、图3-3 SHA256 `187A4C1A8A99E970016AAF505CB5AE310C46C29D0D76AE164358F60FBC8E55C4`、PDF SHA256 `50EC6F4BFC46C7B1287FAF2B835943A959FAD7CD201986CAD490E3400691A8DA`。

### 三项 P1 关闭确认

| 原编号 | 关闭证据 | 结论 |
|---|---|---|
| R-P1-1 | 正文第23行已明确链接属于“上游Linux 5.10概念文档”，并明确不代表厂商5.10.160与上游实现完全相同；设备绑定和功能仍要求板端逐项确认。来源索引第3行同步拆开上游5.10与厂商5.10.160，明确补丁、配置、驱动差异；第7行登记 Rockchip *RV1106 Datasheet* Rev1.8及官方产品页，只支撑启动介质能力；已删除原子操作旧条目；第23行已改为当前“3张框图”。 | **CLOSED** |
| R-P1-2 | 正文第45行已把范围限定为“Linux内核的内存管理、中断处理和文件系统尚未开始工作”，没有再否定BootROM/引导程序可能具有的有限内存、存储或事件处理机制。 | **CLOSED** |
| R-P1-3 | 正文第57行继续保留“身份快照不说明所有外设可用”；第59行只把“已绑定驱动的设备”纳入内核管理，并再次要求逐项测试。未再把设计连接、驱动绑定或系统启动外推成全部硬件实测通过。 | **CLOSED** |

### 最终回归结果

- 当前 PDF 与 Markdown 的修订内容一致。第3～6页的版本边界、启动前限定和外设测试边界均完整显示，没有截字、错页、图文覆盖或旧句残留。
- 正文仍为5节、3张教学图；没有 Linux 命令、固定设备节点、板端写操作、烧录步骤、驱动配置或音频实现。
- `BootROM → 引导程序 → Linux内核 → init → 用户空间` 的职责关系未退化；RV1106启动介质能力仍未被写成boomPI的优先级或失败回退顺序。
- Linux 5.10上游机制、当前板厂商5.10.160身份字符串以及“设备是否实测可用”保持为三层不同证据。

### 仅余非阻断 P2

- 图3-1把设备驱动画成独立层，正文已明确“内核里负责具体硬件的部分叫设备驱动”。以后若优化图形，可把驱动视觉上纳入Linux内核框；当前图不构成事实错误。
- 图3-3把“根文件系统与init”合在一格。正文已经准确说明rootfs是目录、命令、库和配置文件的集合，init才是内核启动的第一个用户空间程序；以后可把rootfs改为侧边物料，但不阻断本版放行。

除非 Markdown、来源索引或 PDF 发生内容变化，否则无需再次技术复审。

## 结构重写后复审（2026-08-10 21:00）

复审对象：重写为5节、3图后的当前 `chapter03.md`、`sources/README.md` 与6页 `chapter03-v0.1.pdf`。
正文 SHA256：`08BA906AAE33314DFC3FBE1C61F3FC86A807CA9804EB3438FCC131091E79C6DE`
来源索引 SHA256：`53B4897C6A734E1DA39F2B6794B1BBA3F7CA46A4A342FED5E483CD40C2939F7B`
PDF SHA256：`F58C2C2AA9E8F9EB815979D07FF7846B2C3D4D4F090A424874776690D4B73466`

### 复审结论

**REJECTED（无 P0，剩余3项 P1）**。

这次结构性重写已经关闭首轮 P1-2、P1-3、P1-4：正文不再把普通用户程序写进 IRQ 上下文，原子操作/锁的整节和图已经删除，也不再把开发工具归为 Linux 内核提供的能力。首轮 P1-1 的五个正文链接已全部换成 `/5.10/` 版本路径，调度、VFS、IRQ、设备模型等来源也已经补入索引；但“上游5.10文档”和“当前板厂商5.10.160”的证据边界还没有在读者可见正文中说透。

六页 PDF 与当前 Markdown 一致，3张图清楚且没有截字、覆盖或错页。章节没有 Linux 命令、固定设备节点、板端写操作、烧录步骤或音频实现。

### 剩余 P1

| 编号 | 定位 | 仍有问题 | 可直接修订 |
|---|---|---|---|
| R-P1-1 | 正文第23、35、41、43、51行；PDF第3～5页；`sources/README.md` 第3、23行 | 正文现在写“Linux 5.10官方……”，第57行又写当前板为 Linux 5.10.160，初学者仍可能把上游5.10文档当成厂商5.10.160补丁集的精确说明。来源索引第3行只说“不把当前Linux主线……套到boomPI的5.10内核”，既没有点明“上游5.10 ≠ 厂商5.10.160”，也把本次实际引用的版本化5.10文档含混称作“当前主线”。此外，第49行的 RV1106 启动介质结论没有在课程来源索引中登记 Rockchip Datasheet Rev1.8；第23行仍写“6张框图”，而当前成品只有3图，且已删除的原子操作仍留在第17行。 | 在正文第23行首次官方链接前加一句：**“以下链接是上游 Linux 5.10 文档，用来解释通用职责；当前板运行字符串虽为厂商 5.10.160，但其补丁、配置和具体行为仍须以对应 BSP 源码与板端证据为准。”** 后文可继续简称“Linux 5.10上游文档”。把来源索引第3行改成同一边界；删除已不用的原子操作条目；将“6张框图”改为“3张教学图”；新增 Rockchip *RV1106 Datasheet*, Rev1.8，第6页及官方产品页，明确它只支撑启动/下载介质能力，不支撑 boomPI 优先级或回退顺序。 |
| R-P1-2 | 正文第45行；PDF第5页，3.4末段 | “板子刚上电时，这三项机制还没有建立”过于绝对。BootROM或引导程序可能已初始化部分内存，也可能有自己的中断/存储读取机制；此处真正尚未启动的是 **Linux内核的** 内存管理、IRQ处理和VFS。 | 将第45行改为：**“板子刚上电时，Linux的内存管理、IRQ处理和虚拟文件系统尚未启动。引导程序先把内核装入内存；内核接管后再初始化这些子系统并挂载根文件系统。”** 不需要在本章展开引导程序自身的实现。 |
| R-P1-3 | 正文第57、59行；PDF第6页末段 | 第57行刚说明“不说明所有外设已经可用”，第59行又写“处理器、内存和外设已经由内核统一管理”，重新把未绑定驱动、未验证或资源所有者未知的外设写成已经受内核管理，前后矛盾，也会把设计事实写成板端通过。 | 将第59行首句改为：**“系统完成启动以后，内核已经建立处理器、内存和设备管理框架；只有成功识别并绑定相应驱动的外设，才可能通过对应接口交给应用使用，实际功能仍需后续逐项测试。”** 后一句可改为“用户空间此时可以看到系统提供的目录和文件”，避免承诺任意应用都有读取权限。 |

### 非阻断建议

- 图3-1仍把“设备驱动”画成 Linux内核框之外的独立层，而正文第23行已经准确写成“内核里负责具体硬件的部分”。若要彻底消除初学者歧义，可把图中绿色框改成“Linux内核：调度、内存、文件、设备驱动”，或将橙色驱动框画在内核外框内部；这不影响本轮其余结论。
- 图3-3仍把“根文件系统与init”放在同一个主链框。正文已正确说明 rootfs 是目录、程序、库与配置的集合；后续配图优化时宜让主链保持 `BootROM → 引导程序 → Linux内核 → init → 用户空间应用`，把 rootfs 作为内核挂载、init取用的侧边物料。
- 第3行“上一章已经把……连接到RV1106G3”可改为“上一章的原理图已经把……连接到RV1106G3”，进一步避免把图纸连接误读成所有器件已经装配、上电并验证。

### 本轮已通过项

- Linux内核与完整Linux系统已经在正文第5行拆开；Ubuntu、Buildroot和开发工具的旧段落已退出本章。
- 程序文件、进程、线程及并发的关系准确，没有声称多线程自动更快或实时，也没有虚构当前项目三进程架构。
- IRQ只作为硬件通知内核的概念介绍，不再出现“普通程序关中断”或在中断上下文睡眠的误导。
- eMMC块访问、文件系统和VFS的层次合理；具体目录与命令已后移。
- BootROM启动介质能力与boomPI实际优先级明确分开；没有虚构失败回退。
- 当前板只写入一次身份/进程快照，并明确不证明BSP可复现和全部外设可用。
- 音频仍后置，本章没有音频设备、参数、命令或测试动作。

完成以上3项 P1、重建 PDF并登记新哈希后，可进入一次定点复核；不需要再次结构重写。

审校日期：2026-08-10（Asia/Shanghai）
审校对象：`docs/course/chapters/ch03/chapter03.md` 与 `chapter03-v0.1.pdf`（8页）
正文 SHA256：`102FCE07BB0D277231BED0BAF79A34B822291980A29631E287D780C58A58BB15`
PDF SHA256：`4CC24C879DED849FF71C174803273E1614E472928E202AF7ADA2AD494D65F933`
内部事实基线：`docs/course/audit/ch03-technical-source-map.md`

## 首轮结论（已被上方结构重写后复审取代）

**REJECTED（无 P0，剩余 4 项 P1）**。

章节主线、8页版面和六张概念图总体成立：没有 Linux 命令、设备节点、板端写操作、烧录动作或音频实现；进程/线程、虚拟内存、系统调用/中断、文件系统以及 `BootROM → bootloader → kernel → init → app` 的主体关系也没有发现 P0 级错误。PDF 与当前 Markdown 内容一致，未见截字、错页或图文覆盖。

暂不放行的原因不是章节结构，而是四处会让初学者建立错误边界：正文直接链接当前上游在线文档却没有交代厂商 5.10.160 的版本边界；把普通用户程序写成能够关闭中断/运行在中断处理上下文；把多步并发同步限定成锁或消息队列；以及把“开发工具”混进 Linux 内核提供的能力。

## P1：放行前必须修正

| 编号 | 定位 | 问题与证据 | 可直接执行的修订 |
|---|---|---|---|
| P1-1 | 正文第17、37、45、57、63行；PDF第3、5、6、7页；`sources/README.md` 第3、7～11行 | 正文五个链接均指向没有版本前缀的 `docs.kernel.org` 当前在线页。当前板只读证据仅确认运行字符串为厂商 Linux `5.10.160`；当前在线页不能证明该厂商内核的实现、配置或补丁行为。即使改用 `/5.10/` 页面，它也只是上游 5.10 文档，不是厂商 5.10.160 补丁集。现有“应用看到的是稳定接口”也过宽，不能把所有私有 `ioctl`、sysfs 属性或调试接口都承诺成稳定 ABI。 | 在第17行首次引用前加入统一边界：**“当前板端快照显示厂商 Linux 5.10.160。下列上游文档用于解释跨版本概念；涉及具体接口、配置和行为时，应以当前 BSP 源码与板端证据为准。”** 把对应链接换成版本化页面：`https://docs.kernel.org/5.10/userspace-api/index.html`、`https://docs.kernel.org/5.10/admin-guide/mm/index.html`、`https://docs.kernel.org/5.10/process/adding-syscalls.html`、`https://docs.kernel.org/5.10/core-api/wrappers/atomic_t.html`、`https://docs.kernel.org/5.10/admin-guide/init.html`。将第17行后半改为：**“应用通过定义明确的用户空间 API 请求内核服务；文档化 UAPI 通常重视兼容性，但不能把所有驱动私有接口或调试接口都视为稳定承诺。”** `sources/README.md` 同步记录“上游 5.10 文档 ≠ 厂商 5.10.160 实现”，并补入本章实际使用的 POSIX、VFS、IRQ、Buildroot、U-Boot/Rockchip 来源；不能继续声称六张图只依据现有五条链接。 |
| P1-2 | 正文第51行；PDF第6页图3-4下方 | “普通教学程序不应长时间关闭中断，也不应在中断处理过程中……”把用户态应用与内核/驱动上下文混在一起。普通用户程序通常没有直接关闭 CPU 中断的权限，也不会作为硬件 IRQ handler 执行；这些约束属于内核/驱动开发。依据 Linux 5.10 `genericirq` 文档，其讨论对象是内核 IRQ 处理路径，不是普通应用。 | 整段替换为：**“普通用户程序不会直接关闭 CPU 中断，也不在硬件中断处理上下文中运行；它通常只看到数据返回、事件通知或任务被唤醒。以后若编写内核或驱动代码，才需要按具体 IRQ 上下文和内核 API 判断哪些操作允许睡眠、哪些处理必须尽快结束。”** |
| P1-3 | 图3-5底部结论；PDF第7页；正文第53、57行 | 图中写“多个步骤必须用锁或消息队列保护”，把同步方法限定为两种，也没有写出“确实存在并发访问”这一前提。Linux/应用设计还可使用单一所有权、事件循环、无锁算法、RCU、信号量等；没有并发访问时也不需要为多步骤一律加锁。正文第57行的“锁、消息队列或其他同步机制”反而比图更准确。 | 把图中结论改为：**“多个步骤若存在并发访问，需要采用适合场景的同步方式，例如锁、消息传递或单一所有权。”** 正文第53行第一句可同步改为“多个执行单元并发访问同一份可变状态时，可能出现竞争”。重新导出概念图和 PDF，避免图文结论互相冲突。 |
| P1-4 | 正文第71行；PDF第8页“3.6 boomPI为什么选择Linux” | “成熟的驱动框架、进程隔离、文件系统、网络协议和开发工具。Linux内核提供这些基础机制”会把编译器、调试器等开发工具归入内核。Linux 内核提供驱动、调度/内存隔离、文件系统和网络等内核机制；开发工具来自主机工具链或用户空间生态。 | 前两句改为：**“这类产品需要成熟的驱动框架、进程与内存隔离、文件系统和网络协议，也需要主机侧编译、调试工具。Linux 内核提供前一组底层机制；工具链和用户空间软件来自相应生态，Buildroot则在开发主机上选择并构建内核、根文件系统及所需软件包。”** |

## P2：建议一并收口

| 编号 | 定位 | 建议 |
|---|---|---|
| P2-1 | 第9行，PDF第2页 | “早期计算机一次只运行一项固定工作”是过度概括的历史叙述，且本章没有历史来源。建议删除年代判断，直接写“当多项程序开始共享处理器、内存和外设时，公共管理工作随之增加”。“操作系统长期驻留”也宜改成“内核长期驻留并承担核心管理职责”，避免把整个操作系统等同于常驻内核。 |
| P2-2 | 第11～17行、图3-1，PDF第3页 | 图把“设备驱动”和“内核”画成两个并列层级，初学者可能理解成驱动在 Linux 内核之外。建议把绿色框改为“Linux内核：调度、内存、文件、设备驱动”，或至少标注“内核中的设备驱动”；用户态驱动是另外的条件性架构，本章无需展开。 |
| P2-3 | 第33行、图3-3，PDF第4～5页 | “每个进程认为自己拥有连续地址”容易被理解成整个地址空间连续且全部驻留物理内存。建议改为“每个进程拥有独立虚拟地址空间，可以申请连续的虚拟地址区间；这些区间不必连续映射到物理页，也不一定同时驻留物理内存”。 |
| P2-4 | 第47行，PDF第6页 | “内核先……再把耗时工作交给后续上下文”是常见 IRQ 设计，而不是每种设备和每次中断都必然采取的固定步骤。句首加“典型情况下”，并把“确认中断”改成“按该控制器/IRQ流程处理或确认事件”，即可保留初学者因果链而不承诺统一实现。 |
| P2-5 | 图3-6，PDF第8页 | 当前图把“根文件系统与init”合成主链执行框。正文已经说明 rootfs 是目录、库和配置的集合，图也应一致：主链保留 `BootROM → 引导程序 → Linux内核 → init → boomPI应用`，把 `root filesystem` 作为供内核挂载、供 init/应用取用的侧边物料。 |
| P2-6 | 第73行，PDF第8页 | “boomPI镜像由Buildroot组织……”可保留设计概念，但应补一条当前证据边界：板端快照只确认 `/etc/os-release` 标识 Buildroot 2023.02.6，不能据此证明当前工作区能够逐字节复现正在运行的镜像，也不能把 Buildroot 写成板上常驻服务。 |

## 已通过的技术边界

- 正文明确本章不执行 Linux 命令、不进入驱动配置；成品中也未出现命令行、固定设备节点或测试结果。
- 没有把 `Linux 5.10.160` 写成未修改的上游内核，也没有宣称 PREEMPT_RT、FIFO/RR、实时延迟、swap 或完整 256 MB 可用内存。
- 进程、线程和调度采用入门教学模型，并明确多线程不自动更快或实时；未虚构 boomPI 项目的线程数与优先级。
- 虚拟内存未被写成“硬盘扩展内存”；256 MB 被标成名义物理容量，并保留板端可用量以后续实测为准。
- 系统调用与硬件中断的触发方向已区分；图3-4没有绑定具体 IRQ、节点或调用次数。
- 文件系统与 eMMC 块设备分层基本正确，且明确拒绝把“一切皆文件”当绝对规则。
- 启动正文没有虚构 eMMC、SD、USB 的 BootROM 优先级或自动回退；也没有把 BootROM、bootloader、kernel 和 init 写成同一角色。
- Linux、Ubuntu 与 Buildroot 的类型区分基本正确；Ubuntu没有被写成板端系统，Buildroot没有被写成另一种内核。
- 音频实现仍后置。本章仅在设备接口举例中出现“声卡”一词，没有命令、设备节点、采样参数或读写动作。

## 复审放行条件

1. 关闭全部 P1，并重新生成概念图、DOCX/PDF。
2. 在正文可见位置写清“上游 5.10 文档、厂商 5.10.160、当前在线文档”三者的证据边界；只改 `sources/README.md` 不足以让读者看到该边界。
3. 重渲染后逐页确认图3-5的新句子未截断、图3-6的 rootfs 不再作为执行角色，且所有版本化链接可点击。
4. 重新登记 Markdown 与 PDF 的完整 SHA256 后，方可给出技术 PASS。
