# 第4章v0.1可读性与低AI味终审

## 结论

**REJECTED。**

五节标题和大方向已经连续：`Linux来历 → Ubuntu/Buildroot关系 → 登录确认身份 → 根目录 → 进程与设备节点`，没有碎成命令词典，也没有重复第3章的操作系统原理。这条主线可以保留。

当前阻断项不在章节骨架，而在“观察边界”没有收住：唯一真实终端图同时展示以太网链路和讲义采集提示；目录、进程部分反而只有概念图；正文又提前写到SD卡挂载、视频节点、触摸、显示、网络和音频。对怕术语的读者来说，这会把“认识Linux系统”变成“硬件测试项目名预告”，也违反硬件逐项测试后移、音频最后实现的课程顺序。

本轮基线：

- `docs/course/chapters/ch04/chapter04.md`，2026-08-10 21:09；
- `docs/course/chapters/ch04/chapter04-v0.1.pdf`，6页；
- PDF SHA-256：`E074A8E3B81D211792D16CB59321D01DC16FB41D898E8D311EFE9A8EC69809C7`；
- `qa/chapter04-v0.1-contact.png`及`qa/v0.1-pages/page-001.png`～`page-006.png`。

## P0：交付前必须关闭

### P0-1：身份截图混入网络测试与采集脚本口吻

位置：4.3、图4-2、PDF第4页。

图中除了`hostname`、`uname`、`os-release`，还出现eth0地址、`carrier=1`、“即完成本关”、`Screenshot ready`和“Press Enter…”等采集提示。图注也写成“身份与以太网链路快照”。这张图虽然可追溯到板端采证，却不是参考文档那种干净的“命令—输出—教师判读”：读者会同时面对身份、网络、验收口号和英文操作提示四条线。

直接执行：

1. 从原图派生裁剪，只保留`cat /etc/hostname`、`uname -srmo`和`cat /etc/os-release`三组命令与输出。
2. 删除正文第35行整段以太网地址、`carrier`、SSH、Wi-Fi、摄像头、触摸和音频说明。
3. 图注改为：

   > 图4-2 本轮板端系统身份快照。主机名来自镜像配置，不代表开发板型号。

4. 如果成品图是根据日志重排而非直接终端抓屏，图注必须写“板端证据终端摘录”，不能写“真实板端会话”。

完成后，本节只回答三件事：主机名是什么、内核怎样自报、用户空间怎样自报，不借身份图提前验收网络。

### P0-2：目录节提前进入存储与硬件测试

位置：4.4第2～5段、图4-3、PDF第4～5页。

正文连续列出`/userdata`、`/oem`、`/mnt/sdcard`、`df -h`、`mount`、ext4、vfat、eMMC、插卡状态、写入临时文件和比较哈希。这些内容已经从“认识一棵目录树”跨到“当前镜像的分区/挂载结论与后续存储测试方案”。读者还没有看到`pwd`和`ls /`的真实输出，却先读到多种挂载和文件系统名称，逻辑顺序倒置。

直接执行：

- 图4-3只保留`/`、`/bin`、`/sbin`、`/etc`、`/dev`、`/proc`、`/sys`、`/tmp`和`/run`；删除`/userdata`、`/oem`、`/mnt/sdcard`。
- 删除“本轮`df -h`与`mount`快照显示……”整段，以及“后面的存储测试会选择……”整段。
- 用下面两段接回主线：

  > 登录后输入`pwd`，可以看到Shell当前位于哪个目录；输入`ls /`，可以看到根目录下的第一层名称。Linux只保留一棵从`/`开始的目录树，不同内容都从这棵树中找到入口。
  > `/bin`和`/sbin`放基础命令，`/etc`放系统配置；`/proc`和`/sys`提供运行中的内核与设备信息，`/dev`提供设备入口。这些名称背后并不都对应eMMC中的普通文件，具体挂载和存储读写留到后续章节实测。

### P0-3：进程与设备节提前点名具体硬件，音频边界回退

位置：4.5、PDF第5页。

`/dev/video14`、摄像头画面、驱动绑定、占用者、格式、触摸、显示、网络和音频在同一段连续出现。虽然正文用的是“不能据此判定”的审慎写法，读者仍被迫提前接触后续六个硬件主题；音频也在课程最后阶段以前第二次被点名。

直接替换为：

> 设备节点是程序进入内核驱动的一类入口。例如`/dev/console`表示控制台入口，`/dev/null`表示一个特殊字符设备。节点能够被列出，只说明系统提供了这个入口；它不是硬件本体，也不能替代后面的功能操作与结果。

本章不要出现`/dev/video*`、`/dev/input/*`、`/dev/snd/*`或具体外设名称。第4章只建立“节点是入口，入口不是功能结果”这一条关系。

### P0-4：标题写“登录后观察”，但目录和进程没有真实观察图

位置：4.4、4.5、图4-3、图4-4、PDF第5～6页。

正文声称“输入`pwd`”“输入`ls /`”“`ps`命令可以读取”，但读者看不到这些命令的实际提示符、输出和应读位置。两节都用概念框图替代了本应存在的板端证据，形成“只有概念，没有操作”。这与《登录》《HelloWorld》《文件传输》中动作后紧邻截图的写法不一致。

最低补图闭环：

1. 一张真实终端截图连续展示`pwd`、`ls /`、`ls -ld /dev /proc /sys`；图后只解释根目录和三个运行入口。
2. 一张真实终端截图展示`ps -ef`表头与前若干行，再展示经本轮核验可用的`cat /proc/1/comm`或等价只读命令；图后只解释PID、PPID、COMMAND和一次PID 1结果。
3. 截图中若出现IP、MAC、账号之外的个人路径或密码，必须裁掉/遮蔽；不得用日志重新排版成假终端。

图4-4“系统线索—继续确认—功能结论”只是在重复正文的证据警告，不能替代进程截图。建议删除该图，把第6页位置交给真实`ps`截图；这样也能关闭尾页留白。

## P1：不阻断结构，但应随P0一并精简

### P1-1：4.2首段形成版本与架构术语墙

位置：PDF第2页末。

同一段连续出现WSL、Ubuntu 26.04 LTS、x86_64、Buildroot 2023.02.6、Linux 5.10.160、armv7l和二进制程序。读者尚未知道“用户空间”，就要同时比较两个版本号、两个架构和三种系统名称。

可直接替换为：

> 课程中的Ubuntu运行在开发电脑上，用来准备编译环境；boomPI板端运行另一套精简系统。两边都使用Linux内核，但处理器架构、程序和库并不相同，因此开发电脑上的程序不能直接复制到板端运行。

下一段再分别解释Ubuntu与Buildroot。Ubuntu版本命名和LTS含义对当前因果链没有帮助，可删；确需保留时放到第5章准备开发环境处。

### P1-2：证据审计口吻重复，教师叙事被“不能/不等于”打断

全文出现“本轮”6次、“不能”4次、“不等于”4次、“不代表”2次。证据边界本身正确，但几乎每个对象后面都跟一条否定，读起来像审计报告。

处理原则：每节保留一条边界即可，其余句子改为正向说明当前能看到什么。例如：

- “这段历史对本课程有一个直接作用……”改为“因此，终端里的Linux版本指向内核；Shell、命令和配置属于内核之外的用户空间。”
- “读者只要认清三件事……”改为“这一次只读取系统信息：先分清开发电脑和开发板，再沿根目录找到命令、运行信息和设备入口。”
- “到这里，读者已经能分清……”删去自我评价，直接进入因果过渡：

  > 终端中的内核、用户空间、目录和进程已经对应起来。下一章再从这些文件怎样组成固件开始，进入构建与烧录流程。

### P1-3：图4-1箭头标签拥挤

位置：PDF第3页。

橙色“内核、根文件系统和软件镜像”分成两行并压到箭头上，视觉上像未排完的标注。可改为一行较短的“系统镜像”，正文继续说明其中包含哪些物料；不要缩小到难读的小字。

### P1-4：第6页信息量不足

位置：PDF第6页。

页面只有一张较大的三框概念图和一个结尾段，下半页空白。删除重复概念图、换入真实进程截图并压缩结尾后，可以让第5、6页重新形成“目录观察—进程观察—收章”的连续图文块；若页数自然缩为5页也可接受。

## 逐页结论

| 页码 | 结论 | 说明 |
|---:|---|---|
| 1 | PASS | 章首页干净，标题层级正确。 |
| 2 | P1 | 历史到Ubuntu/Buildroot的过渡成立；4.2首段术语过密。 |
| 3 | P1 | 图4-1帮助区分主机与板端；箭头文字拥挤，4.3只落一段但未形成孤标题。 |
| 4 | **P0** | 身份截图可读，但混入网络链路、完成本关和采集提示；随后又提前解释硬件测试边界。 |
| 5 | **P0** | 目录图清楚，但正文越界到挂载、SD卡、存储测试；4.5点名视频、触摸、显示、网络与音频。 |
| 6 | **P0/P1** | 只有概念图，没有`ps`真实观察；下半页明显留白。 |

## 复审门禁

- [ ] 图4-2裁成纯身份截图，网络、`carrier`、完成本关和采集提示全部移除。
- [ ] 4.4删除`df`、`mount`、ext4/vfat、SD卡和未来存储测试方案，只保留一棵目录树。
- [ ] 4.5删除具体视频节点和全部硬件名称，音频不再提前出现。
- [ ] 增加根目录与进程的真实终端截图，命令、输出、观察句相邻。
- [ ] 4.2首段拆掉版本/架构术语墙；Ubuntu版本命名后移。
- [ ] 每节只保留一条证据边界，减少审计式否定句。
- [ ] 图4-1箭头标签不重叠；第6页不再是半页空白的概念尾页。
- [ ] 章末只自然引到第5章固件来源，不提前给烧录步骤；硬件逐项测试仍在第6章，音频仍在课程最后。

关闭上述P0后，现有5节顺序可以直接保留，无需重搭章节骨架。

---

## v0.1e定点增量复核（2026-08-10）

### 当前结论：REJECTED，仅剩1项P0

本轮基线：

- `chapter04.md`，2026-08-10 21:21；
- `chapter04-v0.1.pdf`，4页；
- PDF SHA-256：`B68152296492D3E4EDECC759E64A4F64ECA1F8D019567B8594669A22854F2616`；
- `qa/chapter04-v0.1e-contact.png`及`qa/v0.1e-pages/page-001.png`～`page-004.png`。

### 上一轮问题关闭情况

| 上一轮问题 | v0.1e结果 | 说明 |
|---|---|---|
| 身份图混入网络地址、`carrier`、完成本关和采集提示 | 已关闭 | PDF第3页只保留主机名、`uname`和`os-release`区域；正文网络段已删除。 |
| 目录节提前写`df`、`mount`、ext4/vfat、SD卡和存储测试 | 已关闭 | 图4-3与正文只保留根目录、基础命令、配置、运行信息和设备入口。 |
| 提前点名视频、触摸、显示、网络和音频 | 已关闭 | 4.5改用`/dev/console`和`/dev/null`说明普通入口，未再引出后续硬件，也没有音频内容。 |
| 4.2版本/架构术语墙 | 基本关闭 | 正文不再一口气堆叠两个版本和两个架构；主机/板端差异已先用人话说明。图4-1仍保留版本与架构字符串，但分处两端，属于非阻断信息。 |
| 图4-1箭头标签拥挤 | 已关闭 | 箭头只写“系统镜像”，无文字叠压。 |
| 第6页概念尾页和大块留白 | 已关闭 | 全章收敛为4页，第4页连续容纳4.4、4.5及章末过渡，无孤立尾页。 |
| `pwd`、`ls /`、`ps`缺真实操作截图 | **未关闭** | 正文仍要求读者输入命令并引用板端结果，成品却只有目录概念图，没有相应终端输出。 |

### 唯一剩余P0：中央观察动作没有真实终端结果

第4章的出口不是“记住目录名称”，而是登录以后能够观察Linux系统。当前4.3已经用图4-2完成“身份观察”，但4.4和4.5仍停留在概念层：

- 4.4写“登录后输入`pwd`”“输入`ls /`”，图4-3却是教学目录树；读者看不到提示符、当前目录和真实根目录列表。
- 4.5写“`ps`可以读取当前进程快照”“本轮原始记录中PID 1名为`init`”，但没有`ps`输出、表头或PID 1所在行。
- `/dev/console`和`/dev/null`只在正文中出现，没有展示它们怎样在终端中被列出。

这不是“图片越多越好”的版面偏好，而是AGENTS中的操作门禁：操作文字、真实截图、观察结果必须相邻。对于害怕技术词的读者，缺少输出会留下三个无法自行判断的问题：命令该输在哪里、成功输出长什么样、自己的结果与示例不同是否算失败。因此，本项继续阻断PASS。

### 最小修复，不需要扩写正文

只补两张当前板真实终端截图即可，现有5节结构和主要段落不动：

1. 在4.4第一段后放图4-3a，连续展示`pwd`、`ls /`、`ls -ld /dev /proc /sys`及真实输出。原教学目录树可保留为图4-3b，也可删除以控制页数。
2. 在4.5第一段后放图4-4，展示本轮核验可用的`ps`命令、表头、PID 1所在行，以及`cat /proc/1/comm`或等价只读命令的真实结果。不要用旧日志重排成“终端截图”。
3. 每张图后只加一句判读，不新增命令百科：

   > `pwd`给出当前所在目录，`ls /`列出从根目录展开的第一层名称；输出与图中不同并不表示失败，应以当前镜像为准。

   > 这次进程快照中PID 1名为`init`；它只记录当前时刻，不说明具体init实现和长期运行状态。

4. 若命令在当前镜像不支持某个参数，使用本轮实际可用的等价只读命令并同步正文；不要为得到示例输出安装软件或修改系统。

### 4页成品复核

- 第1页：PASS，章首页正常。
- 第2页：PASS，4.1到4.2连续，术语密度已降到可接受范围。
- 第3页：PASS，图4-1清晰，身份图已收敛为一个问题；图注与图片同页。
- 第4页：版面PASS，无裁切、重叠、孤标题和孤立尾页；内容门禁仍因4.4/4.5缺真实截图而未通过。

### 最终判定

**v0.1e的连续性、低AI味、范围边界和4页排版均已通过；只因`pwd`/`ls`/`ps`中央观察动作缺少真实终端截图，当前仍为REJECTED。补齐上述两张证据图并逐页重建后，可直接进入PASS复核，无需再次重写正文。**

---

## v0.1i最终定点复核（2026-08-10）

### 最终结论：PASS

本轮基线：

- `docs/course/chapters/ch04/chapter04.md`；
- `docs/course/chapters/ch04/chapter04-v0.1.pdf`，4页；
- PDF SHA-256：`3949D5ABE00A1A4A42E3FDB9E87B6B31E9579EED156C012E90CB4F18F8AB05F2`；
- `qa/chapter04-v0.1i-contact.png`及`qa/v0.1i-pages/page-001.png`～`page-004.png`。

### 唯一P0已关闭

v0.1e被阻断，并不是因为概念章必须放终端截图，而是当时正文同时要求读者执行`pwd`、`ls`、`ps`并引用“本轮结果”，已经形成操作承诺。v0.1i已经改变了这两节的性质：

- 4.4只解释一棵根目录、几组路径的用途以及它们来自固件或内核运行时的区别；`pwd`、`ls`只作为第6章的实操边界出现，没有要求读者在本章输入，也没有给出当前板结果。
- 4.5只建立“静态文件—进程—PID—驱动入口—设备节点”的关系，没有`ps`命令框、模拟输出、“本轮PID 1”或当前`/dev`节点断言。
- 图4-3明确标为“教学示意”，没有冒充当前板终端证据。

因此，本章现在属于《0.基础概念》《13.Rootfs Bootfs》采用的概念说明写法：先用连续自然段和关系图把对象放到正确位置，真实命令与结果进入后续操作章再闭环。它不再属于《3.登录》《4.分区信息与 Linux 命令》那种必须“命令—截图—判读”相邻的操作段落。`pwd`、`ls`和进程观察由第6章接管，证据债也随操作一起后移，不能再把“第4章未新增截图”判为缺证据。

### 可读性与低AI味复核

- 章首先承接第3章的“内核只能负责管理”，再说明还需要目录、命令、配置和应用；第二段直接限定本章范围，没有学习目标清单或通关模板。
- 五节仍沿`Linux来历 → Ubuntu/Buildroot分工 → 确认登录对象 → 根目录 → 进程与设备入口`推进，后一节解决前一节留下的问题，没有标题碎片。
- Ubuntu、Buildroot、用户空间、PID和设备节点都先说明作用再给名称；没有重新形成版本号或缩写墙。
- 4.3保留一张当前板身份图，只证明主机名、内核和用户空间标识；图中没有网络链路、采集提示或硬件测试结果。
- 4.4和4.5没有视频、触摸、显示、网络、存储或音频项目名预讲；音频仍在课程路线最后阶段。
- 证据边界已经融入教师说明，没有“本轮通过、覆盖率、验收结论”一类审计口吻。
- 4.4和4.5各有一次指向第6章：前者交代目录命令后移，后者交代进程观察与硬件验证后移。短章中略有重复，但两句分别关闭不同操作边界，当前不构成P1，也不值得为删一句重新打乱稳定分页。

### 逐页结果

| 页码 | 结论 | 定点说明 |
|---:|---|---|
| 1 | PASS | 封面层级、日期和页脚正常。 |
| 2 | PASS | 章首、4.1与4.2连续；页面虽以4.2段落中断收尾，但下一页直接续句，不形成孤标题。 |
| 3 | PASS | 图4-1清晰；4.3标题、说明、真实身份图和图注完整同页，没有图文错绑。 |
| 4 | PASS | 4.3判读自然接入4.4；图4-3清晰；4.4到4.5及章末过渡完整，无裁切、重叠、孤标题或孤立尾页。 |

### 最终判定

**PASS。v0.1i已经关闭此前全部可读性P0/P1：概念与操作边界清楚，五节叙事连续，术语负担可控，音频没有提前，4页成品无视觉回归。当前可读性审校不再要求为第4章补拍`pwd`、`ls`、`ps`截图；这些真实操作必须在第6章首次出现时再与当前板证据、结果判读和失败恢复一起完成。**
