# 第4章 v0.1e 技术事实终审

## 结论

**REJECTED（无 P0，剩余 2 项 P1；修正后可直接转 PASS）。**

本结论仅对应以下冻结基线：

- `chapter04.md`：SHA-256 `322EF4C1841B012524EDD312E449D59193C8D740AF941DF1A1C9A21943355403`
- `chapter04-v0.1.pdf`：4 页，SHA-256 `B68152296492D3E4EDECC759E64A4F64ECA1F8D019567B8594669A22854F2616`
- `sources/README.md`：SHA-256 `6C12FE2C31369A6A87D50F0DE848C170D6007BE674B6A538EF48456FDC2D003F`
- 板端身份展示图：SHA-256 `91BA8A2D942E804B633F5FC568E827027F1A13849B9BE9C4F1204FDCF9C504B3`

PDF 已逐页复核。当前四页版已经删除旧候选中的 `df -h`/`mount` 结果、`rkipc`、`/dev/video14`、具体摄像头和音频内容；正文、DOCX/PDF可见内容与当前 Markdown 一致，未再提前作硬件功能结论。

## P0

无。

## P1：发布前必须修正

| 编号 | 位置 | 当前问题 | 直接修文 / 闭环动作 |
|---|---|---|---|
| P1-1 | `chapter04.md` 第17行；PDF第2页 4.2首段 | “开发电脑上的程序不能直接复制到板端运行”把“程序”说成绝对结论。数据文件、脚本以及已为目标架构构建的程序均可能复制后使用；真正需要限制的是为开发电脑本机架构构建的可执行二进制。 | 改为：“两边的处理器架构和可用运行库不同，因此在开发电脑上为本机生成的可执行程序，通常不能直接放到板端运行；后续需要使用面向板端架构的工具链，并核对板端运行库。脚本和数据文件是否可用，要按文件类型另行判断。” |
| P1-2 | `sources/README.md` 第9、15行；图4-1；图4-2 | 事实本身可确认，但发布证据链没有登记完整：本轮只读检查得到 WSL 环境为 Ubuntu 26.04 LTS、`x86_64`，但来源表把“官方发布周期网页”和“本机实测身份”写在同一格，没有本机 `/etc/os-release` 与 `uname -m` 的归档文件/哈希；图4-2当前展示图哈希为 `91BA…04B3`，而旧 `technical-verdict.md` 放行的是已被同名覆盖的 `587C…9673`，不能继续沿用旧图的放行结论。 | 二选一：①归档主机 `cat /etc/os-release`、`uname -m` 的只读输出，记录采集时间和 SHA-256；在来源表分开写“Ubuntu发布规则（官方网页）”与“当前主机身份（本地证据）”。同时登记图4-2当前展示图哈希 `91BA…04B3`，注明它只展示身份字段，底层事实由板端原始记录支撑；②若不补证据，则从图4-1删除“26.04 LTS / x86_64”精确版本，并把来源表“主机实测身份”改成“版本命名说明”。不得把旧 `587C…9673` 当成当前图的哈希。 |

## P2：建议顺手提高精度

1. `chapter04.md` 第33行把 `/etc/os-release` 描述为“查看用户空间由哪套系统生成”略带因果推断。更准确的写法是：“查看当前系统镜像写入的发行版/构建标识”。该文件能给出声明标识，单凭它不能证明当前 BSP 的源码、配置和构建过程可复现。
2. `sources/README.md` 中 Ubuntu release-cycle 页面可以支撑 YY.MM、LTS 和发布周期，但不是“Ubuntu已经组合好软件仓库、包管理、桌面或服务器工具”这句话的最直接来源。建议另列 Ubuntu 官方 About/Documentation 页面，或把正文保留为不依赖具体版本的概念说明。
3. Buildroot正文使用的是稳定的通用职责，现有官方手册足以支撑；若后续写入板端精确版本 `2023.02.6`，应改引本轮 `board-baseline-raw.txt` 的 `/etc/os-release`，不能由当前在线手册版本反推。

## 已通过的重点事实与边界

| 审校项 | 结论 | 证据/理由 |
|---|---|---|
| GNU 与 Linux 历史 | PASS | 正文只说明 GNU 工具与 Linux 内核的层次关系，并明确内核是完整系统的一个组件；没有写成“Linux在1991年一次完成整套操作系统”，也没有进入命名争论。 |
| 上游 5.10 与厂商 5.10.160 | PASS | 正文明示版本号相近不代表源码完全相同，并要求回到厂商补丁、配置和板端结果。 |
| Buildroot角色 | PASS | 正确写成主机侧构建工具，而非板端内核、服务或运行中的构建器；“生成出来的系统在板端运行”的表述成立。 |
| 板端系统身份 | PASS（仅身份快照） | `board-baseline-raw.txt` 记录 Linux 5.10.160、armv7l、Buildroot `VERSION_ID=2023.02.6` 和 `VERSION=-g994243753-dirty`；正文只写主机名、内核、架构与Buildroot标识，没有把它扩大成 BSP 可复现或整板通过。 |
| 目录与挂载 | PASS | 正文只介绍 `/etc`、`/bin`、`/sbin`、`/proc`、`/sys`、`/dev`、`/tmp`、`/run` 的入门职责，并明确它们不都对应 eMMC 普通文件、挂载和读写后置。当前版没有把一次 `mount`/`df` 快照写成存储健康结论。 |
| 进程 | PASS | 只写原始快照中 PID 1 名为 `init`，并明确进程名只是线索；没有再把 `rkipc` 名称解释为课程客户端或功能通过。 |
| 设备节点与 `/dev/video14` | PASS | 当前版只用 `/dev/console`、`/dev/null` 解释“入口”，并明确节点存在不等于硬件本体或功能结果；已删除 `/dev/video14` 和相机格式内容，不会把动态节点名固化。 |
| 硬件/音频前置 | PASS | 当前四页版没有网络连通、存储读写、显示、触摸、相机或音频测试结论；音频保持后置。 |
| 只读安全边界 | PASS | 本章给出的 `pwd`、`ls /`、`ps`、`cat`、`uname` 均为读取动作；没有写配置、挂载、删除、设备流或 mixer 操作。 |

## 审计使用的本地证据

- `tmp/boompi-doc-v5/evidence/board/20260810-dhcp-recovery/board-baseline-raw.txt`，SHA-256 `2A288C875686F2707B017DE1867442F271146FD27C906F2E07C5A202472733FF`
- `tmp/boompi-doc-v5/evidence/board/20260810-dhcp-recovery/board-topology-raw.txt`，SHA-256 `3AF3F994D41DDFDF7EB64609F61DADE851DAB97D0861A084D094F99A78F51540`
- `tmp/boompi-doc-v5/evidence/board/20260810-dhcp-recovery/technical-verdict.md`，SHA-256 `54B938FC9BCE6A56240CCFC53E164A88981818A5FEC71BE23863A85117AD5CE9`

关闭 P1-1 与 P1-2 后，若正文、来源表与展示图之外没有新增技术内容，本章技术审校可转为 **PASS**；无需重新执行任何板端硬件测试。

## 2026-08-10 最终定点复核（v0.1e）

**PASS。前述 2 项 P1 均已关闭；本节结论取代文首的阶段性 `REJECTED`。剩余 P0：0；剩余 P1：0。**

最终放行基线：

- `chapter04.md`：SHA-256 `7B4A41A88CC69F4687166175312FEABD952D8882F249DD18EA82FC7C18F3D430`
- `chapter04-v0.1.pdf`：4 页 A4，SHA-256 `6B317A163918185CEAABC3BB9752FD3F02897694D87F2183B1DD6EAFD77EE638`
- `sources/README.md`：SHA-256 `DA097B06219801A2C7BBA0F053843D543357297390A0C9867CC901695B3D8F53`
- `sources/host-ubuntu-identity-20260810.txt`：SHA-256 `AA5DB2A156304414379ED78CBBFC5892BD3FBFA9A35599E30801A3006BCB47E1`
- `assets/evidence/terminal-board-identity.png`：SHA-256 `91BA8A2D942E804B633F5FC568E827027F1A13849B9BE9C4F1204FDCF9C504B3`

关闭情况：

1. **P1-1 已关闭。** 4.2 已把对象限定为“面向 `x86_64` 编译的可执行文件”，并写成不能直接复制到 armv7l 板端运行；不再把脚本、数据或面向目标架构构建的程序一概包含在内。该修文已进入最终 PDF 第2页，完整可读、无裁断。
2. **P1-2 已关闭。** 新增主机身份只读记录，明确 Ubuntu 26.04 LTS、`x86_64` 与 WSL2 内核身份、采集日期、采集命令和使用边界；`sources/README.md` 已把 Ubuntu 官方发布周期与本机身份文件分开登记，并登记当前板端身份展示图的实际哈希 `91BA…04B3`。不再沿用旧展示图 `587C…9673` 的哈希。

最终 PDF 仍为4页；终审抽查确认修文进入成品，原先已经通过的 GNU/Linux 历史、Buildroot职责、厂商 5.10.160 边界、目录/进程/设备节点只作线索、具体硬件和音频后置等结论均无技术回归。无需重新执行板端硬件测试。

## 2026-08-10 v0.1h 终审：终端操作后移

**PASS。P0：0；P1：0。**

本结论对应：

- `chapter04.md`：SHA-256 `EFB49433F13B8B70854D657092123D4DF30C59754A5A79159870849DE282065F`
- `chapter04-v0.1.pdf`：4页A4，SHA-256 `92FE7E9691A8BF1C83E9750147A987427DBBCF60D6ABD530E4E5823947E208BA`
- `sources/README.md`：SHA-256 `DA097B06219801A2C7BBA0F053843D543357297390A0C9867CC901695B3D8F53`

定点复核结果：

1. 4.4已经删除本章执行 `pwd`、`ls /` 的动作句和当前板输出，只保留从 `/` 开始的目录树、常见目录职责以及 `/proc`、`/sys`、`/dev`与普通固件文件来路不同的概念。正文提到“第6章再用 `pwd` 和 `ls`对应”只是明确后移位置，不构成本章实测结论。
2. 4.5已经删除 `ps` 操作和“本轮PID 1名为init”的当前板结果，只保留“进程有PID、初始化程序占用PID 1、设备节点是驱动入口”的通用Linux概念。没有把任一进程名、PID或节点写成当前硬件功能通过。
3. 图4-3仍明确标为教学示意，没有伪装成当前镜像根目录、挂载或节点实测拓扑。
4. 图4-2继续只证明2026-08-10那次会话的hostname、Linux 5.10.160/armv7l和Buildroot标识；正文没有把它扩展为第5章未来烧录镜像的验收结果。
5. 当前版没有 `mount`、`df`、存储容量、`rkipc`、具体视频/输入/音频节点或硬件PASS；具体硬件与音频后置边界无回归。
6. PDF逐页可见内容与Markdown一致，4.4、4.5及章末后移说明完整，无旧 `pwd/ls/ps` 实测段落残留。

第4章技术门禁已经通过。以下是**跨章依赖**，不构成本章P1，但必须在整书顺序中关闭：

- 第3章结尾仍承诺“下一章登录并观察目录、进程和存储空间”，应同步改成第4章认识概念、第6章实操；
- 第5章若保留板端部署/运行HelloWorld，必须先给最小终端前提，或把HelloWorld板端运行移到第6章系统基线之后；
- 第6章必须重新采集 `pwd`、`ls`、挂载和 `ps` 的命令、真实输出、截图与判定；若第5章烧录新镜像，不能复用旧镜像快照充当新镜像结果；
- 完整命令、证据门禁和各硬件项目边界见 `audit/ch04-defer-terminal-technical.md`。

## 2026-08-10 v0.1i 最终候选定点复核

**PASS。P0：0；P1：0。本节为第4章最终放行基线，并取代v0.1h PDF基线。**

- `chapter04.md` 未再改动：SHA-256 `EFB49433F13B8B70854D657092123D4DF30C59754A5A79159870849DE282065F`
- 最终 `chapter04-v0.1.pdf`：4页A4，SHA-256 `3949D5ABE00A1A4A42E3FDB9E87B6B31E9579EED156C012E90CB4F18F8AB05F2`

增量检查确认：PDF第2页开头已经使用当前Markdown的“本章先分清开发电脑和开发板……网络配置、文件写入和硬件测试留到后面的实操章节”，不再残留旧候选中的“这一次只读取系统信息”。其余第1、3、4页渲染与v0.1h一致；第4页4.4/4.5仍为纯概念，`pwd`/`ls`只作为第6章未来动作被点名，`ps`操作和当前PID 1结果没有返回第4章。没有技术或证据边界回归。

整书层面的第3章过渡、第5章HelloWorld依赖和第6章实测闭环仍按 `ch04-defer-terminal-technical.md`执行；它们不影响本章v0.1i技术放行。
